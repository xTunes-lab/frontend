export enum StarStatus {
    empty,
    full,
    half,
}

export enum RepeatMode {
    All,
    Single
}


export enum ThemeMode {
    Light = "light",
    Dark = "dark"
}

export enum NotificationType {
    MusicUpdate = 0,
    PlaylistUpdate = 1
}
export enum OperationType {
    Add = 0,
    Update = 1,
    Delete = 2
}
export enum SongEditTabName {
    Detail = "detail",
    Artwork = "artwork",
    Lyrics = "lyrics",
    Options = "options",
    Sorting = "sorting",
    File = "file"
}